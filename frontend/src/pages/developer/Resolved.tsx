import{useState}from"react";import{AppShell}from"@/layouts/Sidebar";import{PageHeader}from"@/layouts/PageHeader";import{SearchInput}from"@/components/ui/Input";import{TicketTable}from"@/components/ui/TicketTable";import{PriorityBadge}from"@/components/ui/Badge";import{PageError,PageLoading}from"@/components/ui/PageState";import{useTickets}from"@/hooks/useData"
export function DeveloperResolved(){const{tickets,loading,error}=useTickets(),[search,setSearch]=useState("");if(loading)return<AppShell role="developer"><PageLoading/></AppShell>;if(error)return<AppShell role="developer"><PageError message={error}/></AppShell>;const rows=tickets.filter(t=>t.statusDb==="resolved"&&(t.subject.toLowerCase().includes(search.toLowerCase())||t.ticket_number.includes(search)));return<AppShell role="developer"><div className="max-w-6xl px-8 py-8"><PageHeader title="Resolved Tickets" subtitle="View support requests you've completed."/><SearchInput className="mb-6" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search resolved tickets..."/><TicketTable tickets={rows} columns={[{key:"ticket_number",label:"Ticket ID",render:t=>`#${t.ticket_number}`},{key:"subject",label:"Subject"},{key:"category",label:"Category"},{key:"priority",label:"Priority",render:t=><PriorityBadge priority={t.priority}/>},{key:"updated",label:"Resolved Date"},{key:"requestor",label:"Requestor"}]} detailRoute={id=>`/developer/tickets/${id}`}/></div></AppShell>}
